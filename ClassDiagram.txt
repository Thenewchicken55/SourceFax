[XMLParser| +content:String; +doneReading:bool; +qName:String; +prefix:String; +localName:String; +characters:String | +XMLParser(content:String); +isDoneReading():bool;+setDoneReading(newValue:bool);+getQName():String;+setQName(newQName:String);+getPrefix():String; getLocalName():String; +inEscape():bool;+getcharacters():String; +parseXMLDeclaration(); +parseVersion();+parseEncodingAndStandalone();+parseDOCTYPE();+refillPreserve():int;+parseCharacterEntityReference();+ parseCharacterNotEntityReference(); +parseComment():int; +parseCDATA(, doneReading:bool):int; +parseProcessing();+parseEndTag(); +parseStartTag():std::size_t;+parseNamespace(); +parseAttribute(); +isXML():bool; +isCDATA():bool; +isComment():bool; +isNamespace():bool; +isCharacter(in index:int, in character:char):bool; +removePrefix(in indexToRemove:int); +findFirstNotOf(delimiters:String):size_t; +sizeOfContent():int; +npos():size_t; +compareContent(in startingPosition:int, in lengthOfString:int, in compareToString: String):int; +isMatchNameMask():bool]